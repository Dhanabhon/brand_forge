name: brand_forge
description: "A flutter package to change app name and icon via command line."
version: 0.0.4
homepage: https://github.com/Dhanabhon/brand_forge

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  args: ^2.7.0
  path: ^1.9.1
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.24.0
  